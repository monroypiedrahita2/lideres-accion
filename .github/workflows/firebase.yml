name: Deploy Angular to Firebase

on:
  push:
    branches:
      - main
      - develop
      - medellin

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Generate prod environment from secrets (when main)
        if: github.ref_name == 'main'
        run: |
          cat > src/environment.prod.ts <<EOF
          export const environment = {
            production: true,
            firebase: {
              apiKey: "${{ secrets.FIREBASE_API_KEY_PROD }}",
              authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN_PROD }}",
              projectId: "${{ secrets.FIREBASE_PROJECT_ID_PROD }}",
              storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET_PROD }}",
              messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID_PROD }}",
              appId: "${{ secrets.FIREBASE_APP_ID_PROD }}",
              measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID_PROD }}",
            },
            collections: {
              comunas: 'Comunas',
              referidos: 'Referidos',
              iglesias: 'Iglesias',
              roles: 'Roles',
              lideres: 'Lideres',
              barrios: 'Barrios',
              perfil: 'Perfiles',
              vehiculos: 'Vehiculos',
              casasApoyo: 'Casas',
              testigos: 'Testigos',
              puestosVotacion: 'PuestosVotacion',
              cuentavotos: 'Cuentavotos',
              carreras: 'Carreras',
            },
            alerts: { timeOut: 3000, preventDuplicates: true },
          };
          EOF

      - name: Generate dev environment from secrets (when develop)
        if: github.ref_name == 'develop'
        run: |
          cat > src/environment.dev.ts <<EOF
          export const environment = {
            production: false,
            firebase: {
              apiKey: "${{ secrets.FIREBASE_API_KEY_DEV }}",
              authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN_DEV }}",
              projectId: "${{ secrets.FIREBASE_PROJECT_ID_DEV }}",
              storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET_DEV }}",
              messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID_DEV }}",
              appId: "${{ secrets.FIREBASE_APP_ID_DEV }}",
              measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID_DEV }}",
            },
            collections: {
              comunas: 'Comunas',
              referidos: 'Referidos',
              iglesias: 'Iglesias',
              roles: 'Roles',
              lideres: 'Lideres',
              barrios: 'Barrios',
              perfil: 'Perfiles',
              vehiculos: 'Vehiculos',
              casasApoyo: 'Casas',
              testigos: 'Testigos',
              puestosVotacion: 'PuestosVotacion',
              cuentavotos: 'Cuentavotos',
              carreras: 'Carreras',
            },
            alerts: { timeOut: 3000, preventDuplicates: true },
          };
      - name: Generate medellin environment from secrets (when medellin)
        if: github.ref_name == 'medellin'
        run: |
          cat > src/environment.medellin.ts <<EOF
          export const environment = {
            production: false,
            firebase: {
              apiKey: "${{ secrets.FIREBASE_API_KEY_MEDELLIN }}",
              authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN_MEDELLIN }}",
              projectId: "${{ secrets.FIREBASE_PROJECT_ID_MEDELLIN }}",
              storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET_MEDELLIN }}",
              messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID_MEDELLIN }}",
              appId: "${{ secrets.FIREBASE_APP_ID_MEDELLIN }}",
              measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID_MEDELLIN }}",
            },
            collections: {
              comunas: 'Comunas',
              referidos: 'Referidos',
              iglesias: 'Iglesias',
              roles: 'Roles',
              lideres: 'Lideres',
              barrios: 'Barrios',
              perfil: 'Perfiles',
              vehiculos: 'Vehiculos',
              casasApoyo: 'Casas',
              testigos: 'Testigos',
              puestosVotacion: 'PuestosVotacion',
              cuentavotos: 'Cuentavotos',
              carreras: 'Carreras',
            },
            alerts: { timeOut: 3000, preventDuplicates: true },
          };
          EOF

      - name: Build Angular
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            npm run build -- --configuration=production
          elif [ "${GITHUB_REF_NAME}" = "medellin" ]; then
            npm run build -- --configuration=medellin
          else
            npm run build -- --configuration=development
          fi

      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          projectId: ${{ github.ref_name == 'main' && secrets.FIREBASE_PROJECT_ID_PROD || (github.ref_name == 'medellin' && secrets.FIREBASE_PROJECT_ID_MEDELLIN || secrets.FIREBASE_PROJECT_ID_DEV) }}
          firebaseServiceAccount: ${{ github.ref_name == 'main' && secrets.FIREBASE_SERVICE_ACCOUNT_PROD || (github.ref_name == 'medellin' && secrets.FIREBASE_SERVICE_ACCOUNT_MEDELLIN || secrets.FIREBASE_SERVICE_ACCOUNT_DEV) }}
          channelId: live